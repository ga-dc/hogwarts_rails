<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div class="student">
    <h2><%= link_to student.name, student %></h2>
    <%= image_tag student.img_url %>
    <!-- <%= link_to student.name, students_path(student) %> - <%= student.house.name %> -->
</div>
<% end %>
