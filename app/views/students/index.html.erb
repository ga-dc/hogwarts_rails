<h1>Here are the students</h1>
<% @students.each do |student| %>
<div class="student">
    <%= image_tag student.img_url %>
    <div><%= link_to student.name, student_path(student) %> - <%= student.house %></div>

</div>
<% end %>
