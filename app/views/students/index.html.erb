<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <p><%= link_to student.name, students_path(student) %></p>
    <p><%= link_to @houses.name, houses_path %></p>
    <p> <img src="<%= student.img_url %>"> </p>
<% end %>
  </div>
