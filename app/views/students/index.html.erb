<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <%= link_to student.name, students_path(student) %> - <% find_house = student.house_id %><%= @houses.find(find_house).name %>
     </div>
<% end %>
