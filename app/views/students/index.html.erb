<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <% house = House.find(student.house_id).name %>
    <%= link_to student.name, student_path(student) %> - <%= house %>
<% end %>
  </div>
