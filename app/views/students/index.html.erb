<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div class="student">
    <h2><%= link_to student.name, student_path(student) %> - <%= link_to student.house.name, house_path(student.house.name) %> </h2>

  </div>
  <% end %>