<h1>Here are the students</h1>
<% @student.each do |student| %>
  <div class="student">
    <h2><%= link_to "#{student.name}", student_path(student) %> - <%= student.house.name %></h2>
    <img src="<%= student.img_url %>"/>
  </div>
<% end %>
