<h1>Here are the students</h1>
<% @students.each do |students| %>
  <div>
    <%= link_to student, students_path(student) %> - <%= student.house %>
<% end %>
  </div>


<!--
  <h1>The houses</h1>
  <%@houses.each do |house|%>
    <div class="house">
      <h2><%= houses_path %></h2>
      <img src="<% house.img_url %>"/>
    <div>
  <% end %> -->
