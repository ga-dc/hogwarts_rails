<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div class = "student">
    <div><%= link_to "#{student.name}", student_path(student) %></div>
    <div><%= student.house.name %></div>
    <%= image_tag student.img_url %>
  </div>
<% end %>
