<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <h2><%= student.name%> </h2>
    <%= image_tag student.img_url %>
    <%= link_to student.name, student %> - <%= link_to student.house.name, student.house %>
  </div>
<% end %>
