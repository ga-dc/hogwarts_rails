<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <%= link_to student.name, student %>
    <%= link_to student.house_id, student %>
    <img src="<%= student.img_url %>"/>
  </div>
<% end %>
