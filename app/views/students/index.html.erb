<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <%= link_to student.name, student %> - <%= student.house.name %>
    <a href="/students/<%= student.id %>">
      <img src="<%= student.img_url %>" alt="<%= student.name %>" >
<% end %>
  </div>
