<h2><%= @student.name %></h2>
<h3><%= @student.house.name %></h3>
<div class="student">
  <%= image_tag @student.img_url %>
</div>

<%= form_for @student, method: :delete do |f| %>
  <%= f.submit "Remove Student" %>
<% end %>
