<h1>Enroll a New Student</h1>

<%= form_for @student do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :img_url %>
  <%= f.text_field :img_url %>

  <%= f.label :house %>
  <%= f.text_field :house %>

  <%= f.submit "Create" %>
<% end %>
