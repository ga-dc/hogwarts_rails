<h1>The houses</h1>
<% @houses.each do |house| %>
  <div class="house">
    <h2><%= house.name %> <%= link_to "View House", house_path(house) %></h2>
    <img src="<%= house.img_url %>"/>
  <div>
<% end %>
