<h1>The houses</h1>

<% @houses.each do |house| %>
  <div class="house">
    <h2><%= link_to house.name, house_path(house)%></h2>
    <img src="<%= house.img_url %>"/>
  </div>
<% end %>





<!-- <% @houses.each do |house| %>
  "Hello"
  <div class="house">
    <h2><a href="houses_path"</a>@houses</h2>
    <img src="<%= house.img_url %>"/>
  <div>
<% end %> -->
