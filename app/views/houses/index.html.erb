<h1>The houses</h1>

<div class="house">
<% @houses.each do |house| %>
  <div class="house">
    <h2>
      <%= link_to house.name, house %>
    </h2>
    <img src="<%= house.img_url %>"/>
  </div>
<% end %>
</div>
