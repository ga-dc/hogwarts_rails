<h2>The houses</h2>
<% @houses.each do |house| %>
  <div class="house">
    <h3>  <%= link_to "#{house.name}", house_path(house) %></h3>
    <img src="<%= house.img_url %>" alt="house.name"/>
  </div>
<% end %>
