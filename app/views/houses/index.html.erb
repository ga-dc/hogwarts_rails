<h1>The houses</h1>
<% @houses.each do |house| %>
  <div class="house">
    <li>
      <%= link_to house.name, house %>
      <a href="/houses/<%= house.id %>">
        <img src="<%= house.img_url %>" alt="<%= house.name %>" >
      </a>
    </li>
  <div>
<% end %>
