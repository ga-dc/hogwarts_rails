<%= link_to "View Houses", houses_path %>
<h1>The houses</h1>
<ul>
  <% @houses.each do |house| %>
  <p><%= house.name %></p>
  <p><%= house.created_at %></p>
  <p><%= house.updated_at %></p>
  <img src="<%= house.img_url %>"/>
  <div>
    <% end %>
  </ul>
