<h2><%= @house.name %></h2>
<% @house.students.each do |student| %>
  <div class="student">
    <h2><%= link_to "#{student.name}", student_path(student) %></h2>
    <%= image_tag student.img_url, alt: "#{student.name}" %>
  </div>
 <% end %>
